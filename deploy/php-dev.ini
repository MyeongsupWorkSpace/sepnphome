; Minimal PHP dev config to avoid extension load errors
; No extensions loaded here

; Show only errors we care about
display_errors=Off
html_errors=Off
error_reporting=E_ERROR

; Timezone
date.timezone=Asia/Seoul

; Disable output compression to avoid proxy/SSE issues
zlib.output_compression=0
